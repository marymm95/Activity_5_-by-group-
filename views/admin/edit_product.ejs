<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <%- include('../partials/link') %>
</head>
<body class="bg-gray-50">
    <div class="flex h-screen">
        <aside class="w-64 bg-gray-800 text-gray-100">
            <%- include('../partials/sidebar') %>
        </aside>

        <div class="flex-1 overflow-auto">
            <header class="bg-white border-b border-gray-200 px-8 py-6">
                <div class="flex justify-between items-center">
                    <h1 class="text-2xl font-bold text-gray-900">Edit Product</h1>
                </div>
            </header>

            <main class="p-8">
                <div class="bg-white rounded-lg shadow-lg p-6">
                    <div class="border-b border-gray-200 mb-4">
                        <h2 class="text-lg font-semibold text-gray-900">Edit Product Details</h2>
                    </div>
                    <form action="/admin/products/edit/<%= product.id %>" method="post" enctype="multipart/form-data">
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                            <div>
                                <label class="block text-gray-700 font-medium">Product Name</label>
                                <input type="text" name="name" value="<%= product.name %>"
                                    class="w-full mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                            </div>
                            <div>
                                <label class="block text-gray-700 font-medium">Description</label>
                                <textarea name="description" rows="4"
                                    class="w-full mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required><%= product.description %></textarea>
                            </div>
                            <div>
                                <label class="block text-gray-700 font-medium">Price</label>
                                <input type="number" step="0.01" name="price" value="<%= product.price %>"
                                    class="w-full mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                            </div>
                            <div>
                                <label class="block text-gray-700 font-medium">Stock</label>
                                <input type="number" name="stock" value="<%= product.stock %>"
                                    class="w-full mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                            </div>
                            <div>
                                <label class="block text-gray-700 font-medium">Category</label>
                                <input type="text" name="category" value="<%= product.category %>"
                                    class="w-full mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                            </div>
                            <div>
                                <label class="block text-gray-700 font-medium">Image</label>
                                <input type="file" name="image"
                                    class="w-full mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                            </div>
                            <div class="flex items-center">
                                <img src="/images/<%= product.image %>" alt="<%= product.name %>" class="w-20 h-20 rounded-md object-cover mt-2">
                                <p class="text-gray-500 ml-2"><%= product.image %></p>
                            </div>
                        </div>
                        <button type="submit"class="mt-6 bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition duration-300">Update Product</button>
                        <a href="/admin/products" class="text-blue-600 font-semibold hover:underline">back to Products</a>
                    </form>
                </div>
            </main>
        </div>
    </div>

    <% if (productMessage) { %>
        <script>
            $(document).ready(function() {
                toastr.success("<%= productMessage %>", 'Success');
            });
        </script>
    <% } %>

    <%- include('../partials/script') %>

</body>
</html>
